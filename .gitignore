# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (keep the template)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# raw source assets — the compressed versions live in public/photos/
/Logo/
/Photos/

# per-machine Claude Code config (keep the shared launch.json)
/.claude/settings.local.json
